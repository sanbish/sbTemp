@model IEnumerable<MyInventory.Data.Item_Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Category)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.E_Approval)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Category)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.E_Approval)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.C_Code }) |
            @Html.ActionLink("Details", "Details", new { id=item.C_Code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.C_Code })
        </td>
    </tr>
}

</table>
